


def run_model(config):


    print('recieved config',config)


	#now=datetime.datetime.now().strftime('%Y-%m-%d')
	# date
	# time
	# simname
	# folder name
	# progress bar
	# compute_time
	# size
	# ready
	# simDuration

	# currentDT = datetime.datetime.now()

	# stringdate=currentDT.strftime("%Y-%m-%d")
	# stringtime=currentDT.strftime("%H:%M:%S")


	# db_initial_row([stringdate,stringtime,config['simName'],config['folderName'],0,config['compute_time'],0,False,0])
	# compute_time=config['compute_time']

	# world=World(config)

	# simName=config['simName']
	# folderName=config['folderName']

	# updateSchedule=makeUpdateSchedule(compute_time)

	# print(updateSchedule)
	# print()


	# for t in tqdm(range(1,int(compute_time+1))):
	# 	world.advance_plancktime(t)

	# 	if t in updateSchedule:
	# 		alter_row(folderName,t)






	# return world.export_data(config)
